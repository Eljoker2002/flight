import 'package:flutter/material.dart';

class ShowOneFormAndPrint extends StatelessWidget {
  ShowOneFormAndPrint({Key? key, required this.traineeName}) : super(key: key);

  String traineeName;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
